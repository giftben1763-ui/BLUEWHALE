---
title: "Design Principles"
description: "The philosophy behind the library."
icon: "compass"
---

Bluewhale is built on four core principles to ensure it is the most reliable tool for deposit routing.

### 1. Never Throw
The library functions are "pure" in the sense that they should never throw an exception. Errors are returned as values within the result object. This makes it easier to use in high-availability backend services.

### 2. Errors are Values
By returning errors and warnings as structured data rather than throwing, we force the developer to consider the edge cases of Stellar address handling.

### 3. Canonical Output
The library always returns data in a standardized format:
- **Addresses**: Always uppercase (G..., M...).
- **Routing IDs**: Always a string representation of a `uint64`, with no leading zeros.

### 4. The Spec is the Product
The heart of the project is the `vectors.json` specification. The TypeScript, Go, and Dart packages are simply implementations of that spec. If a new edge case is found, it is added to the spec first, and then all implementations are updated.

<Info>
  This approach ensures that a wallet built in Flutter and a backend built in Go will always agree on where a deposit should go.
</Info>

<!-- PARITY_MATRIX_BEGIN -->
<!-- Generated by scripts/generate-parity-matrix.js — do not edit manually -->
<!-- Last generated: 2026-09-25 -->

### Cross-Platform Feature Parity

The table below is auto-generated by `scripts/generate-parity-matrix.js`.
Run `node scripts/generate-parity-matrix.js` to refresh it after adding a feature.

| Feature | Description | TypeScript | Go | Dart/Flutter |
| ------- | ----------- | :--------: | :-: | :----------: |
| Address detection (`Detect` / `detectAddress`) | Classify a raw string as G, M, or C address | ✅ | ✅ | ❌ |
| Address parsing (`Parse` / `parse`) | Full parse into a typed result with warnings | ✅ | ✅ | ❌ |
| Address validation (`Validate` / `validate`) | Boolean validity check for a Stellar address | ✅ | ✅ | ✅ |
| Muxed address encoding | Encode a G-address + uint64 ID into an M-address | ✅ | ✅ | ✅ |
| Muxed address decoding | Decode an M-address back to G-address + uint64 ID | ✅ | ✅ | ✅ |
| Routing extraction (`extractRouting`) | Resolve deposit routing ID from address + memo | ✅ | ✅ | ✅ |
| Precision-safe routing ID (`SafeRoutingId`) | Lossless uint64 wrapper for JS/Flutter Web | ✅ | ✅ | ✅ |
| Routing ID JSON as string | routingId serializes as a decimal string, never a raw integer | ✅ | ❌ | ✅ |
| Warning system | Structured, non-throwing warnings on edge cases | ✅ | ✅ | ✅ |
| SEP-0029 memo requirement check | Flag when a destination account requires a routing memo | ✅ | ✅ | ✅ |
| Spec test-vector runner | Consume vectors.json to validate implementation | ✅ | ✅ | ✅ |
| URI / SEP-0007 routing extraction | Extract routing info from a SEP-0007 payment URI | ✅ | ❌ | ❌ |

> **Parity summary:** TypeScript 12/12 · Go 10/12 · Dart/Flutter 9/12
<!-- PARITY_MATRIX_END -->
